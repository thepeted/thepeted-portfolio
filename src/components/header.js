import React from 'react';

import Button from './button';

export default () => {
  return (
    <header>
      <div className="nav">
        <Button title="Get in touch" url="#"/>
      </div>
    </header>
  )
}
